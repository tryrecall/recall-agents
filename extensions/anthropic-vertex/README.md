# SteelEngine Anthropic Vertex Provider

Official SteelEngine provider plugin for Claude models hosted through Google Vertex AI.

Install from SteelEngine:

```bash
steelengine plugin add @steelengine/anthropic-vertex-provider
```

Configure Google Cloud credentials and the target Vertex project/region in SteelEngine, then select Claude models with the Anthropic Vertex provider.
