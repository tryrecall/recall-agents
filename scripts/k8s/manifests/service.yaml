apiVersion: v1
kind: Service
metadata:
  name: steelengine
  labels:
    app: steelengine
spec:
  type: ClusterIP
  selector:
    app: steelengine
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
