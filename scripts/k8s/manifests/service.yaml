apiVersion: v1
kind: Service
metadata:
  name: recall
  labels:
    app: recall
spec:
  type: ClusterIP
  selector:
    app: recall
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
