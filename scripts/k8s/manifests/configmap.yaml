apiVersion: v1
kind: ConfigMap
metadata:
  name: recall-config
  labels:
    app: recall
data:
  recall.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.recall/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "Recall Assistant",
            "workspace": "~/.recall/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # Recall Assistant

    You are a helpful AI assistant running in Kubernetes.
