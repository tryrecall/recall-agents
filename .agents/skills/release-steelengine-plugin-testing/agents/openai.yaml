interface:
  display_name: "SteelEngine Plugin Pre-Release Testing"
  short_description: "Plan plugin release validation"
  default_prompt: "Use $release-steelengine-plugin-testing to plan or run pre-release SteelEngine plugin validation across package, lifecycle, doctor, gateway, SDK, and live-ish proof."
