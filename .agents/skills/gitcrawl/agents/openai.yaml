interface:
  display_name: "Gitcrawl"
  short_description: "Search local SteelEngine issue and PR history before live GitHub triage"
  default_prompt: "Use $gitcrawl to inspect SteelEngine issue and PR history, find related threads and duplicate candidates, then verify actionable decisions with live GitHub."
