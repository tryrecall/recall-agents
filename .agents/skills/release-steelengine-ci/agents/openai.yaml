interface:
  display_name: "SteelEngine Release CI"
  short_description: "Verify and debug SteelEngine release validation runs"
  default_prompt: "Use $release-steelengine-ci to validate one frozen release SHA with one active parent run, recover only the smallest failed lane, and stop on a precise external blocker."
